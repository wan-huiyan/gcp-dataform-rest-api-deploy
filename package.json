{
  "name": "gcp-dataform-rest-api-deploy",
  "version": "1.1.0",
  "private": true,
  "description": "Deploy .sqlx files to Google Cloud Dataform via REST API — covers writeFile, commit, push, compile, invoke, and poll lifecycle with gotcha documentation",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "type": "module"
}
